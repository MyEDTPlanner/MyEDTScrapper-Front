import {
    Box,
    Typography
} from '@mui/material';

export const PreviewTabPanel = () => {
    return (
        <Box  sx={{ p: 3 }}>
            <Typography>Apperçu</Typography>
        </Box>
    );
}